"""Schedules — cron-style triggers.

Empty as of v0.2.0. Future:
- daily_beach_refresh_schedule (replaces pg_cron `daily_beach_refresh_nightly` at 09:00 UTC)
- hourly_now_refresh_schedule (replaces `hourly-beach-now-refresh` pg_cron)
"""
