# scripts/dbt — Dog Beach Scout dbt projects

Subdirectory for any dbt project this app uses.

Currently:

- [`dog_beach/`](./dog_beach) — the main pipeline project (sources + staging + marts).

See `scripts/dagster/dog_beach/` for the procedural-orchestration counterpart.
